/* TATNERA — subscription & billing settings */
(function(){'use strict';
const FN='https://ayxvspeufbsoxtccaqap.supabase.co/functions/v1/stripe-customer-portal';let busy=false;
function fmt(v){if(!v)return'–';try{return new Intl.DateTimeFormat('de-DE',{day:'2-digit',month:'2-digit',year:'numeric'}).format(new Date(v))}catch(_){return'–'}}
function role(){try{return String(window.TatneraAuth?.role?.()||window.TatneraAuth?.membership?.()?.role||'')}catch(_){return''}}
async function render(){const settings=document.getElementById('settings'),auth=window.TatneraAuth,id=auth?.studioId?.(),db=auth?.client;if(!settings||!id||!db)return;let panel=document.getElementById('tatneraBillingPanel');if(!panel){panel=document.createElement('section');panel.id='tatneraBillingPanel';panel.className='panel space-top';settings.appendChild(panel)}const{data}=await db.from('studios').select('subscription_status,stripe_customer_id,stripe_subscription_id,stripe_current_period_end,stripe_cancel_at_period_end').eq('id',id).single();const active=data?.subscription_status==='active',cancel=!!data?.stripe_cancel_at_period_end,allowed=['owner','admin'].includes(role());panel.innerHTML=`<div class="panel-head"><div><span class="eyebrow">Abo & Abrechnung</span><h3>TATNERA Studio-Abo</h3></div></div><div style="display:grid;gap:12px"><div><strong>${active?'19 € / Monat':'Kein aktives Abo'}</strong><div class="muted" style="margin-top:5px">${cancel?`Gekündigt · Zugang bis ${fmt(data?.stripe_current_period_end)}`:active?`Aktiv${data?.stripe_current_period_end?` · nächste Abrechnung ${fmt(data.stripe_current_period_end)}`:''}`:'Nach der Testphase nur bei aktiver Buchung kostenpflichtig.'}</div></div>${active&&allowed?'<div><button type="button" class="btn primary" data-billing-manage>Abo & Zahlung verwalten</button><p class="muted" style="margin:8px 0 0;font-size:12px">Zahlungsmethode, Rechnungen und Kündigung werden sicher über Stripe verwaltet.</p></div>':''}</div>`;panel.querySelector('[data-billing-manage]')?.addEventListener('click',openPortal)}
async function openPortal(e){if(busy)return;busy=true;const btn=e.currentTarget,auth=window.TatneraAuth;btn.disabled=true;btn.textContent='Stripe wird geöffnet …';try{const session=(await auth.client.auth.getSession()).data.session;if(!session)throw new Error('session');const r=await fetch(FN,{method:'POST',headers:{Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({studioId:auth.studioId()})});const out=await r.json();if(!r.ok||!out.url)throw new Error(out.detail||out.error||'portal');location.href=out.url}catch(err){console.error(err);alert('Die Abo-Verwaltung konnte nicht geöffnet werden. Bitte erneut versuchen.');btn.disabled=false;btn.textContent='Abo & Zahlung verwalten';busy=false}}
document.addEventListener('tatnera:auth-ready',()=>setTimeout(render,250));document.addEventListener('click',e=>{if(e.target.closest('[data-view="settings"],[data-view-target="settings"]'))setTimeout(render,150)});document.addEventListener('tatnera:runtime-refresh',()=>{if(document.getElementById('settings')?.classList.contains('active-view'))render()});window.TatneraBilling={render};})();